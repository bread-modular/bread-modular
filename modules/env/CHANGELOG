# Changelog

## 1.2.0
Introduce the MIDI_VELOCITY mode
This is the 3rd option in the MIDI_GATE button.
If so, the LED will blink at 500ms apart.
As of the functionality, it will modulate the CV2 with velocity changes.

## 1.1.0
Use a better button

## 1.0.0
Release 1.0.0

## 0.1.2
Load the gate mode config on setup()
So, it will load the correct gate mode on across MCU restarts.

## 0.1.1
Use the correct pins for LEDs.
Our early prototype(gate module) didn't had those pins.
So, we update it when we get the 0.1.0 prototype.

## 0.1.0
Initial Release of ENV
